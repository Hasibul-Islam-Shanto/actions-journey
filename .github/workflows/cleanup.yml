name: Cleanup Checking
on:
  push:

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Start test database
        run: echo "Starting test database"
      - name: Flaky check
        run: exit 1
      - name: Stop test database
        if: always()
        run: echo "Stopping test database"
